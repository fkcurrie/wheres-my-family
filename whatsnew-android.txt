• Resolved a critical startup crash affecting Android devices running API levels below 26 (Android 8.0) by switching to a fully backward-compatible Base64 cryptographic encoder/decoder.
• Fixed a Google Maps initialization exception by dynamically injecting the map API key via Manifest Placeholders.
• Improved app stability and startup sequence on all supported Android versions.
